<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block mb-5 " href="/logout">
                <button>Logout</button>
            </a>
        </div>
        <h3 class="text-3xl"><span class="font-light">Hello </span>, <%= user.name %> 🙌</h3>
        <h5 class="mb-5">You Can Create A New Post</h5>
        <form method="post" action="/post">
            <textarea class="w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none block" placeholder="Whats on Your Mind? 😏" name="content" id=""></textarea>
            <input class="px-3 py-2 bg-yellow-500 w-40 rounded-md mt-2 block" type="submit" value="Create New Post" name="">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/2 mb-2 p-4 rounded-md border-[1px] border-zinc-700">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %>!</p>
                    <small class="mt-1 inline-block">
                        <%= post.likes.length %> likes
                    </small>
                    <div class="btns flex gap-4 mt-2">
                        <a class="text-blue-500" href="/like/<%= post._id %>">Like</a>
                        <a class="text-yellow-500" href="">edit</a>
                    </div>
                </div>
                <% }) %>

            </div>
        </div>
    </div>
</body>
</html>